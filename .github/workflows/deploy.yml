name: CI / Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt pytest

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t deep-agents-enhanced:latest .

      - name: Test image health
        run: |
          docker run -d --name test-app \
            -e OPENAI_API_KEY=dummy \
            -e TAVILY_API_KEY=dummy \
            -p 8501:8501 deep-agents-enhanced:latest
          sleep 20
          docker exec test-app curl --fail http://localhost:8501/_stcore/health || true
          docker stop test-app && docker rm test-app

      # ── Deployment targets (uncomment the one you use) ──────────────────
      # Railway:
      # - name: Deploy to Railway
      #   uses: bervProject/railway-deploy@main
      #   with:
      #     railway_token: ${{ secrets.RAILWAY_TOKEN }}
      #     service: deep-agents-enhanced

      # Render (trigger deploy hook):
      # - name: Deploy to Render
      #   run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}

      # AWS ECR + ECS:
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: us-east-1
      # - name: Push to ECR
      #   run: |
      #     aws ecr get-login-password | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}
      #     docker tag deep-agents-enhanced:latest ${{ secrets.ECR_REGISTRY }}/deep-agents-enhanced:latest
      #     docker push ${{ secrets.ECR_REGISTRY }}/deep-agents-enhanced:latest
